{
  "type": "sqlite",
  "database": "./sqlite.db",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true,
  "migrations": ["migration/*.js"],
  "cli": {
    "migrationsDir": "migrations"
  }
}